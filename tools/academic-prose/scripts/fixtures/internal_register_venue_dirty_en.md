# Introduction

This paper presents a method for estimating label reliability.
The work is intended for publication in a Q1 journal, so the evaluation
covers two additional datasets.

# Method

We add an ablation because reviewers will likely ask whether each stage
contributes. Targeting a high-impact journal requires broader coverage.

# Results

Accuracy reaches 0.847 on a held-out set of 1,204 items.

# Discussion

To avoid a desk reject we also report a per-stratum error analysis.
