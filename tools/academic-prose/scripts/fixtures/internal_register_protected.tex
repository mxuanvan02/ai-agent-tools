% TODO: nhớ kiểm tra lại bảng này trước khi gửi — comment nội bộ, không phải văn xuôi.
% Theo yêu cầu của anh, đổi thứ tự hai mục.
\section{Kết quả}

Độ chính xác trên tập kiểm tra đạt 55,43\%, cao hơn thiết lập không ngữ cảnh 4,2 điểm phần trăm.
Ngưỡng phân loại được đặt tại $\text{TODO} = 0$ trong công thức $y = \sigma(Wx + b)$.

\begin{verbatim}
# Theo yêu cầu của anh: chạy lại pipeline
python3 /Users/van/run_eval.py --config config.py --out v9_final.csv
# TODO: thêm seed
\end{verbatim}

Bộ dữ liệu được công bố kèm mã nguồn \cite{main.tex2024} tại \url{https://example.org/TODO/dataset}.
Bảng~\ref{tab:main} và Mục~\ref{sec:method} trình bày chi tiết quy trình xây dựng dữ liệu.
